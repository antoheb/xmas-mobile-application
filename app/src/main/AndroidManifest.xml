<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hebert.xmaslist">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/ChristmasAppTheme">
        <activity android:name=".Wish.AddWishActivity"></activity>
        <activity android:name=".Gift.AddGiftActivity" />
        <activity android:name=".Gift.UpdateGiftActivity" />
        <activity android:name=".Gift.GiftDetailsActivity" />
        <activity android:name=".Wish.UpdateWishActivity" />
        <activity android:name=".Wish.WishDetailsActivity" />
        <activity android:name=".Gift.GiftActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".OptionsMenu" />
        <activity android:name=".Wish.WishActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>